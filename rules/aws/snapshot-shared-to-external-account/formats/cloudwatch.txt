fields @timestamp, userIdentity.arn, requestParameters.snapshotId, requestParameters.createVolumePermission, sourceIPAddress
| filter eventSource = "ec2.amazonaws.com"
| filter eventName = "ModifySnapshotAttribute"
| filter ispresent(requestParameters.createVolumePermission.add.userIds)
| sort @timestamp desc
