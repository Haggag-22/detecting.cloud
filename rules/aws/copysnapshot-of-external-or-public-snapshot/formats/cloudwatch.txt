fields @timestamp, userIdentity.arn, requestParameters.sourceSnapshotId, requestParameters.sourceRegion, requestParameters.destinationRegion, recipientAccountId, sourceIPAddress
| filter eventSource = "ec2.amazonaws.com"
| filter eventName = "CopySnapshot"
| filter ispresent(requestParameters.sourceSnapshotId)
| sort @timestamp desc
