{
  "whyItMatters": "Detects enumeration of AWS security services (GuardDuty, Config, Security Hub, WAF) used by adversaries to identify defensive controls before disabling or evading them.",
  "threatContext": {
    "attackerBehavior": "Detects enumeration of AWS security services (GuardDuty, Config, Security Hub, WAF) used by adversaries to identify defensive controls before disabling or evading them.",
    "whyItMatters": "Detects enumeration of AWS security services (GuardDuty, Config, Security Hub, WAF) used by adversaries to identify defensive controls before disabling or evading them.",
    "riskAndImpact": "Abuse related to GuardDuty can weaken detection coverage, enable persistence, or expand attacker access."
  },
  "telemetryValidation": {
    "requiredLogSources": [
      "AWS CloudTrail"
    ],
    "requiredFields": [
      "eventName",
      "userIdentity.arn",
      "eventSource",
      "eventTime",
      "sourceIPAddress"
    ],
    "loggingRequirements": [
      "CloudTrail management events enabled for the relevant AWS APIs"
    ],
    "limitations": [
      "May require environment-specific tuning to reduce operational false positives"
    ]
  },
  "logicExplanation": {
    "humanReadable": "Detects enumeration of AWS security services (GuardDuty, Config, Security Hub, WAF) used by adversaries to identify defensive controls before disabling or evading them.",
    "whenToFire": "When CloudTrail shows activity matching this AWS Security Software And Service Discovery detection."
  },
  "quality": {
    "signalQuality": 6,
    "falsePositiveRate": "Medium until tuned",
    "expectedVolume": "Varies by environment",
    "productionReadiness": "experimental"
  },
  "communityConfidence": {
    "accurate": 0,
    "needsTuning": 0,
    "noisy": 0
  },
  "deployment": {
    "whereItRuns": [
      "SIEM",
      "Athena",
      "EventBridge + Lambda"
    ],
    "scheduling": "Near-real-time preferred for high/critical severity"
  }
}
