fields @timestamp, userIdentity.arn, eventName, requestParameters.projectName, sourceIPAddress
| filter eventSource = "codebuild.amazonaws.com"
| filter eventName = "StartBuild"
| sort @timestamp desc
