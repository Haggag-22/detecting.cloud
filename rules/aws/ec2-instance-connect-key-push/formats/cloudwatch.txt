fields @timestamp, userIdentity.arn, eventName, requestParameters.instanceId, sourceIPAddress
| filter eventSource = "ec2-instance-connect.amazonaws.com"
| filter eventName = "SendSSHPublicKey"
| sort @timestamp desc
