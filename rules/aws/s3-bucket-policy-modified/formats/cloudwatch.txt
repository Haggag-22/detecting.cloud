fields @timestamp, userIdentity.type, userIdentity.arn, eventName, requestParameters.bucketName, requestParameters.policy, sourceIPAddress
| filter eventName in ["PutBucketPolicy", "DeleteBucketPolicy"]
| sort @timestamp desc
