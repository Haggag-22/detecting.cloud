{
  "whyItMatters": "Detects reverting an EC2 instance to a prior state via root volume replacement or snapshot restore (CreateReplaceRootVolumeTask, RestoreSnapshotFromRecycleBin, RestoreSnapshotTier). Adversaries revert instances to undo defensive changes or hide activity (Revert Cloud Instance).",
  "threatContext": {
    "attackerBehavior": "Detects reverting an EC2 instance to a prior state via root volume replacement or snapshot restore (CreateReplaceRootVolumeTask, RestoreSnapshotFromRecycleBin, RestoreSnapshotTier). Adversaries revert instances to undo defensive changes or hide activity (Revert Cloud Instance).",
    "whyItMatters": "Detects reverting an EC2 instance to a prior state via root volume replacement or snapshot restore (CreateReplaceRootVolumeTask, RestoreSnapshotFromRecycleBin, RestoreSnapshotTier). Adversaries revert instances to undo defensive changes or hide activity (Revert Cloud Instance).",
    "riskAndImpact": "Abuse related to EC2 can weaken detection coverage, enable persistence, or expand attacker access."
  },
  "telemetryValidation": {
    "requiredLogSources": [
      "AWS CloudTrail"
    ],
    "requiredFields": [
      "eventName",
      "userIdentity.arn",
      "eventSource",
      "eventTime",
      "sourceIPAddress"
    ],
    "loggingRequirements": [
      "CloudTrail management events enabled for the relevant AWS APIs"
    ],
    "limitations": [
      "May require environment-specific tuning to reduce operational false positives"
    ]
  },
  "logicExplanation": {
    "humanReadable": "Detects reverting an EC2 instance to a prior state via root volume replacement or snapshot restore (CreateReplaceRootVolumeTask, RestoreSnapshotFromRecycleBin, RestoreSnapshotTier). Adversaries revert instances to undo defensive changes or hide activity (Revert Cloud Instance).",
    "whenToFire": "When CloudTrail shows activity matching this AWS EC2 Instance Revert Via Root Volume Replacement detection."
  },
  "quality": {
    "signalQuality": 6,
    "falsePositiveRate": "Medium until tuned",
    "expectedVolume": "Varies by environment",
    "productionReadiness": "experimental"
  },
  "communityConfidence": {
    "accurate": 0,
    "needsTuning": 0,
    "noisy": 0
  },
  "deployment": {
    "whereItRuns": [
      "SIEM",
      "Athena",
      "EventBridge + Lambda"
    ],
    "scheduling": "Near-real-time preferred for high/critical severity"
  }
}
